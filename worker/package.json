{
  "name": "commentkit-worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply commentkit-db --local --env dev",
    "db:migrate": "wrangler d1 migrations apply commentkit-db",
    "db:execute": "wrangler d1 execute commentkit-db --remote --command",
    "db:execute:local": "wrangler d1 execute commentkit-db --local --env dev --command",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.6",
    "hono": "^4.11.3",
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251229.0",
    "@types/bun": "^1.3.5",
    "typescript": "^5.9.3",
    "wrangler": "^4.54.0"
  }
}